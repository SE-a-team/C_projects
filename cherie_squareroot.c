#include <math.h>
/**
 *main-calculates the square root of a number
 *Return: 0
 */
int main()
{
double num = 1.2e3, num__floor, num_root;
num_floor = floor(num);
num_root = sqrt(num);
// ...
return 0;
}
